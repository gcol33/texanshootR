# Inspect the achievement registry

Returns a data.frame of all achievements with unlock status. Hidden
achievements appear with `name = "???"` and `hint = NA` until unlocked.

## Usage

``` r
achievements()
```

## Value

A data.frame.

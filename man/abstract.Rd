% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/output_abstract.R
\name{abstract}
\alias{abstract}
\title{Generate a one-paragraph deadpan abstract}
\usage{
abstract(run, output_dir = NULL, file = NULL, force = FALSE)
}
\arguments{
\item{run}{A \code{tx_run} object returned by \code{\link[=shoot]{shoot()}}. Must be the
currently active publication chain — i.e., the run that opened
the chain when it cleared \code{p <= 0.05}.}

\item{output_dir}{Optional output directory.}

\item{file}{Optional filename stem (without extension).}

\item{force}{Overwrite an existing file.}
}
\value{
Character path to the written file (invisible).
}
\description{
The entry-tier output. Always the first stage of the publication
chain (see \code{\link[=progress]{progress()}}). Writes a plain-text \code{.txt} file describing
the highlighted specification in the unbothered register typical of
an applied-stats abstract.
}
\details{
Chain stage: \strong{abstract} (length 1, always unlocked).
}
